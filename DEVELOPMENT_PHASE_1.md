# المرحلة الأولى من التطوير

هذه النسخة تنفّذ أول حزمة من خطة الأمان والاستقرار.

## ما تغير

- أُلغي منح دور مالك المنصة لهوية ثابتة، وأُغلقت دالة المطالبة العامة بالدور.
- مُنعت حسابات العملاء من إنشاء الفواتير والمدفوعات وأرصدة المحافظ ونقاط الولاء مباشرة.
- أصبحت كتابة المخزون والعمليات المالية مقصورة على الأدوار الإدارية.
- أضيف تحقق خادمي لعزل حجوزات العملاء داخل الصالون الصحيح، مع جلب السعر والمدة من جدول الخدمات.
- مُنع العرض العام لإعدادات الرواتب والحجز والبيانات الإدارية، وأضيفت دوال عامة محدودة الحقول للموقع العام.
- أوقفت واجهات الشحن والتحويل والاستبدال الوهمية إلى حين ربط بوابة دفع ومسارات خادمية ذرّية.
- أصبحت أخطاء المزامنة توقف العملية وتبقي التغييرات قابلة لإعادة المحاولة.
- أُصلح `package-lock.json`، ونُسّق المشروع، وأضيفت اختبارات أمان أولية.

## قبل النشر

1. خذ نسخة احتياطية من قاعدة البيانات.
2. راجع جميع سجلات `platform_owner` وتأكد من هوية كل حساب. لم تحذف migration أي مالك حالي تجنبًا لقفل الإدارة.
3. طبّق migration `20260801180000_security_phase1.sql` في بيئة تجريبية أولًا.
4. اختبر حسابات مالك المنصة، مدير الصالون، الموظف، والعميل على بيانات تجريبية.
5. لا تضع ملف `.env` في المستودع؛ استخدم `.env.example` وأسرار بيئة الاستضافة.

## الفحوصات الناجحة

- `npm ci`
- `npm run lint`
- `npx tsc --noEmit`
- `npm run build`
- `npm test` — أربعة اختبارات ناجحة

## المرحلة التالية

- إنشاء RPC ذري لإنشاء حجز العميل بدل مزامنة حالة المتصفح.
- ربط بوابة دفع حقيقية باستخدام webhook موثوق.
- إنشاء معاملات خادمية للمحفظة والولاء والفواتير والمخزون.
- إضافة اختبارات تكامل فعلية لسياسات RLS باستخدام قاعدة Supabase تجريبية.
