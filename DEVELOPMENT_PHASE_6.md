# المرحلة السادسة — استكمال لوحة التاجر

## النتيجة

أصبحت لوحة مالك المشغل تغطي التشغيل اليومي وإدارة الفروع والمصروفات والتقارير والاشتراك، مع إزالة تدفق شحن البطاقة التجريبي واستبدال عمليات المحفظة الحساسة بمعاملات آمنة داخل PostgreSQL.

## الأقسام الجديدة

- **الفروع:** إنشاء وتعديل وتفعيل وإيقاف الفروع، مع رقم الهاتف والعنوان ونطاق الحضور الجغرافي. حد الفروع مفروض من الباقة في قاعدة البيانات.
- **المصروفات:** سجل مالي للمصروفات مع التصنيف والتاريخ وطريقة الدفع والمورد ومرجع الإيصال.
- **التقارير:** الإيراد المحصل والمصروفات والصافي التشغيلي ومتوسط الفاتورة والحجوزات والعملاء الجدد والخدمات الأكثر طلباً ضمن فترة يحددها التاجر.
- **الاشتراك والباقة:** حالة الاشتراك، السعر، تاريخ الانتهاء، الميزات، واستهلاك حدود الفروع والموظفين والخدمات والعملاء.

## إصلاحات الأمان والبيانات

- حُذف نموذج إدخال البطاقة الذي كان يحاكي نجاح الدفع بلا مزود دفع حقيقي.
- `adjust_customer_wallet` ينفذ التعديل اليدوي مع تحقق الصلاحية والرصيد والقفل الذرّي.
- `transfer_customer_wallet` يحول الرصيد بين عميلين من المشغل نفسه داخل معاملة واحدة.
- `redeem_customer_loyalty` يخصم النقاط ويضيف قيمتها إلى المحفظة داخل معاملة واحدة.
- جدول `merchant_expenses` معزول حسب المشغل بواسطة RLS ولا يقبل فرعاً تابعاً لمشغل آخر.

## الملفات المحورية

- `supabase/migrations/20260801230000_merchant_operations.sql`
- `src/routes/_authenticated/branches.tsx`
- `src/routes/_authenticated/expenses.tsx`
- `src/routes/_authenticated/reports.tsx`
- `src/routes/_authenticated/subscription.tsx`
- `src/merchant-dashboard.test.ts`

## التشغيل

يجب تطبيق ترحيل قاعدة البيانات الجديد على بيئة Supabase قبل اختبار المصروفات أو عمليات المحفظة من المتصفح. لا تُدخل بيانات بطاقات في التطبيق؛ يلزم دمج مزود دفع معتمد مستقبلاً عبر واجهة خادم وWebhooks موثوقة.
