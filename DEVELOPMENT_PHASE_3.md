# المرحلة الثالثة: الصلاحيات الخادمية وقابلية التوسع

تنقل هذه المرحلة ضوابط الباقات والإحصاءات الأساسية من واجهة المتصفح إلى قاعدة البيانات، وتمنع تجاوز حدود الباقة حتى عند تزامن الطلبات.

## ما تم تنفيذه

- إضافة الدالة الخادمية `salon_module_enabled` للتحقق من:
  - تفعيل الباقة.
  - حالة الاشتراك وتاريخ انتهائه.
  - عدم إيقاف المشغل.
  - وجود الوحدة المطلوبة ضمن `enabled_modules`.
- فرض الوحدات المفعلة عبر سياسات RLS تقييدية على جداول الحجوزات والخدمات والمخزون والموظفين والحضور والرواتب والعملاء والكوبونات والفواتير.
- إضافة triggers كطبقة حماية ثانية لكل عمليات الإضافة والتعديل والحذف في جداول الوحدات.
- تقوية حدود الفروع والموظفين والخدمات والعملاء بقفل معاملات يمنع تجاوز الحد بسبب عمليات إنشاء متزامنة.
- إبقاء مالك المنصة وعمليات `service_role` الموثوقة قادرين على الإدارة والاسترداد.
- إضافة إحصاءات مجمعة خادميًا للوحة مالك المنصة بدل تحميل جداول كاملة إلى المتصفح.
- إضافة بحث وترشيح وpagination خادمي للمشاغل والاشتراكات وسجل التدقيق.
- عرض بريد منفّذ العملية في سجل التدقيق بدل الاكتفاء بمعرّف المستخدم.
- منع فتح صفحات الوحدات غير المشمولة في الباقة عبر رابط مباشر من واجهة الإدارة.

## ترحيل قاعدة البيانات

طبّق migrations بالترتيب، بعد نسخة احتياطية وفي بيئة تجريبية أولًا:

1. `20260801180000_security_phase1.sql`
2. `20260801190000_platform_owner_controls.sql`
3. `20260801200000_platform_scale_and_entitlements.sql`

لم يتم تطبيق migration تلقائيًا على قاعدة البيانات الحية.

بعد التطبيق اختبر كل باقة بحساب منفصل، خصوصًا:

- محاولة فتح وحدة غير مفعلة وقراءة بياناتها أو تعديلها.
- الوصول إلى الحد الأقصى ثم إرسال عمليتي إنشاء متزامنتين.
- اشتراك منتهي أو متأخر أو مشغل موقوف.
- وصول مالك المنصة إلى الإحصاءات والسجل والقوائم ذات الصفحات.

## الفحوصات الناجحة

- `npx tsc --noEmit --pretty false`
- `npm run lint`
- `npm test` — 12 اختبارًا ناجحًا في 3 ملفات
- `npm run build`

## تكاملات تحتاج إعدادًا خارجيًا

لم تتم إضافة دفع تجريبي أو webhook شكلي. الربط المالي الحقيقي يحتاج اختيار مزود الدفع، حساب التاجر، مفاتيح بيئة الاختبار، سر توقيع webhook، وسياسة الاسترداد والضرائب. كذلك يحتاج التحقق الآلي من النطاقات إلى معرفة مزود الاستضافة وقيمة DNS المطلوبة وآلية إصدار SSL.

## المرحلة المقترحة التالية

- ربط بوابة الدفع المختارة وإنشاء فواتير اشتراك موثوقة مع webhook idempotent.
- التحقق الآلي من DNS وSSL بدل تغيير `domain_status` يدويًا.
- تشغيل اختبارات تكامل RLS فعلية ضد مشروع Supabase تجريبي.
- إضافة مراقبة للأخطاء ومقاييس تشغيل وتنبيهات للمدفوعات المتأخرة.
