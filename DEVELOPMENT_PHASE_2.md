# المرحلة الثانية: أدوات مالك المنصة

تضيف هذه النسخة أدوات تشغيل وإدارة مباشرة لمالك المنصة، مع سجل تدقيق وضوابط أقوى على الصلاحيات والخطط.

## ما تم تنفيذه

- إضافة قسم **الاشتراكات** مع مؤشرات للحسابات النشطة والتجريبية والمتأخرة والقريبة من الانتهاء.
- إضافة البحث والتصفية حسب حالة الاشتراك، وتجديد الاشتراك 30 يومًا، وتعليقه أو إعادة تشغيله.
- إضافة قسم **سجل التدقيق** لعرض تغييرات الصالونات والخطط وعضويات مالك المنصة، مع القيم قبل التغيير وبعده.
- تطوير قسم **ملاك المنصة** ليعرض جميع الملاك، ويمنح الصلاحية بالبريد الإلكتروني، ويسمح بسحبها بضوابط أمان.
- منع مالك المنصة من سحب صلاحية نفسه، ومنع حذف آخر مالك للمنصة.
- منع تكرار عضوية `platform_owner` للمستخدم نفسه.
- منع حذف خطة مستخدمة أو تغيير رمزها أثناء ارتباط صالونات بها.
- إضافة تأكيدات قبل تعليق صالون أو حذف خطة أو سحب صلاحية مالك.
- إضافة قيود تحقق أساسية لحالات الاشتراك وأسعار الخطط والوحدات المتاحة.

## ترحيل قاعدة البيانات

الواجهة الجديدة تعتمد على migration التالي:

`supabase/migrations/20260801190000_platform_owner_controls.sql`

قبل تطبيقه:

1. خذ نسخة احتياطية من قاعدة البيانات.
2. طبّق أولًا `20260801180000_security_phase1.sql` إن لم يكن مطبقًا.
3. جرّب migration المرحلة الثانية في بيئة تجريبية.
4. تحقق من وجود مالك منصة واحد على الأقل ومن صحة حساباته.
5. اختبر منح الصلاحية وسحبها وتعديل الاشتراكات وسجل التدقيق.

هذه الحزمة لا تطبق migration تلقائيًا على قاعدة البيانات الحية.

## الفحوصات الناجحة

- `npm run lint`
- `npx tsc --noEmit --pretty false`
- `npm test` — ثمانية اختبارات ناجحة في ملفين
- `npm run build`

## ما يوصى به للمرحلة التالية

- فرض حدود الخطة والوحدات المفعلة خادميًا عبر RLS/RPC، وليس في الواجهة فقط.
- ربط بوابة دفع حقيقية مع webhook موثوق وسجل فواتير للمنصة.
- إضافة إحصاءات مجمعة خادميًا بدل تحميل عدد كبير من السجلات إلى المتصفح.
- إضافة pagination وترشيح خادمي لسجل التدقيق والاشتراكات.
- إضافة إدارة النطاقات المخصصة والتحقق من DNS وشهادة SSL.
- إضافة اختبارات تكامل فعلية على مشروع Supabase تجريبي لكل دور وصلاحية.
