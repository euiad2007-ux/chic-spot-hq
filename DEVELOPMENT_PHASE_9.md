# المرحلة التاسعة: صلاحيات الفريق وسجل نشاط التاجر

## النتيجة

أضيفت طبقة صلاحيات دقيقة لمدير الفرع وصفحتان جديدتان لمالك المشغل:

- **صلاحيات الفريق**: يحدد المالك المجالات التي يستطيع كل مدير فرع إدارتها.
- **سجل النشاط**: يعرض آخر التغييرات الإدارية ومن نفذها ومتى، من دون إظهار القيم الحساسة.

## الصلاحيات المتاحة

| الصلاحية           | المجال                                  |
| ------------------ | --------------------------------------- |
| `view_reports`     | التقارير وسجل النشاط                    |
| `manage_bookings`  | الحجوزات والتقويم                       |
| `manage_customers` | العملاء                                 |
| `manage_services`  | الخدمات                                 |
| `manage_inventory` | المخزون والموردون                       |
| `manage_staff`     | الموظفون والحضور والإجازات              |
| `manage_payroll`   | الرواتب                                 |
| `manage_finance`   | الفواتير والمدفوعات والمصروفات والمحافظ |
| `manage_pos`       | نقطة البيع والصندوق                     |
| `manage_marketing` | الكوبونات والتسويق                      |
| `manage_branches`  | الفروع                                  |
| `manage_settings`  | إعدادات المشغل والحجز                   |

## الحماية

- تخزن الصلاحيات في `salon_member_permissions` مع قائمة قيم مسموح بها وقواعد RLS.
- لا يستطيع مستخدم `authenticated` الكتابة المباشرة في جدول الصلاحيات؛ التعديل يمر عبر دالة المالك المحمية.
- تتحقق مشغلات قاعدة البيانات من صلاحية مدير الفرع قبل الإضافة أو التعديل أو الحذف في الجداول التشغيلية.
- لا يستطيع مدير الفرع تعديل عضويات الفريق أو صلاحياتها.
- الحسابات القديمة لمديري الفروع تحتفظ بوصولها السابق حتى يحفظ المالك إعداداً مخصصاً، لمنع توقف التشغيل بعد تطبيق الترحيل.
- سجل النشاط يحذف `national_id` قبل حفظ النسخة السابقة أو اللاحقة.

## الملفات الرئيسية

- `supabase/migrations/20260801260000_merchant_permissions_and_audit.sql`
- `src/routes/_authenticated/team-access.tsx`
- `src/routes/_authenticated/activity-log.tsx`
- `src/components/salon/app-shell.tsx`
- `src/lib/account.ts`
- `src/merchant-permissions-audit.test.ts`

## التطبيق

طبّق ترحيلات Supabase بالترتيب، ثم ابنِ المشروع:

```bash
supabase db push
npm install
npm run test
npm run build
```

إذا لم توجد بيانات أعضاء فعلية فلن تظهر بطاقات مدير فرع في صفحة الصلاحيات. أنشئ حساب مدير فرع واربطه بالمشغل أولاً؛ لا تنشئ الصفحة حسابات دخول وهمية.

## نطاق سجل النشاط

يسجل الترحيل تغييرات الفروع والخدمات والمخزون والموظفين والعملاء والحجوزات والفواتير والمصروفات والإعدادات وصلاحيات الفريق. يعرض حالياً آخر 100 عملية، مع مرشح لنوع العملية.
